#include <stdio.h>
#include <stdlib.h>
int main (){
    scanf("%d%d%d", &a, &b, &c);
    if (a > b , c)
    printf ("maximum = %d\n", a);
    else if (b > a , c)    
    printf ("maximum = %d\n",b);
    return 0;
}
